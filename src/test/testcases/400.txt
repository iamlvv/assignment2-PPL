fact: function integer (n: integer) {
            while (true) {
                    delta: integer = fact(3);
                    inc(x, delta, alpha);
                    do {printHello();}
                    while (1 + 4 == 2);
                }
            }