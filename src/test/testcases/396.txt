fact: function integer (n: integer) {
            while ((n || 4) && (n == 3)) {
                    delta: integer = fact(3);
                    inc(x, delta, alpha);
                    do {printHello();}
                    while (1 + 4 == 2);
                }
            }